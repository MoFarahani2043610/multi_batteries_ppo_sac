# m3_configs/ppo_scaling.yaml
# ============================
# Milestone 3 — PPO scalability sweep config
# Used by: m3_scripts/scale_sweep.py

algo: ppo

env:
  # n_batteries is overridden per cell by scale_sweep.py
  n_batteries: 1
  dt_hours: 0.08333           # 5-minute timesteps
  degradation_penalty: 0.0
  normalize_obs: true
  price_ref: 52.0             # real CAISO mean price $/MWh
  price_source: caiso         # use real CAISO data
  caiso_path: cache/caiso_2024_2026_5min.parquet

training:
  total_timesteps: 1000000    # 1M steps per cell — equal compute budget
  n_seeds: 5                  # 5 seeds per (algo, N) cell
  eval_freq: 10000            # evaluate every 10K steps
  eval_episodes: 10
  log_interval: 1

ppo:
  learning_rate: 0.001        # tuned in M2
  n_steps: 2048
  batch_size: 64
  n_epochs: 10
  gamma: 0.99
  gae_lambda: 0.95
  clip_range: 0.2
  clip_range_vf: null
  ent_coef: 0.001             # tuned in M2 — 0.05 causes collapse
  vf_coef: 0.5
  max_grad_norm: 0.5
  policy: MlpPolicy
  policy_kwargs:
    net_arch:
      pi: [400, 300]          # tuned in M2
      vf: [400, 300]

logging:
  log_dir: m3_logs/ppo_sweep
  tensorboard: true
  save_freq: 100000           # save checkpoint every 100K steps
  verbose: 1